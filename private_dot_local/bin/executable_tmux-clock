#!/usr/bin/env bash
SESSION=$(tmux display-message -p '#S')
FLAGFILE="/tmp/tmux-clock-${SESSION}"
touch "$FLAGFILE"
trap 'rm -f "$FLAGFILE"' EXIT
trap '' INT
while [[ -f "$FLAGFILE" ]]; do
    tmux clock-mode -t "$TMUX_PANE"
    sleep 0.05
done
